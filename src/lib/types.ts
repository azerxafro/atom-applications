export interface FormData {
  fullName: string;
  email: string;
  position: string;
  resumeLink: string;
  whyJoinUs: string;
}

export interface SheetResponse {
  success: boolean;
  error?: string;
}